# /gen-tests

Generate JUnit 5 + Mockito unit tests for a Java class or method.

## Usage
```
/gen-tests [paste Java class or method]
```

## What it does
Invokes the `java-unit-test-generator` skill. Produces a complete test class covering happy paths, edge cases, and error scenarios using Given/When/Then structure.
